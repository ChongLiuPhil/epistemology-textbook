# 编辑与贡献约定

本项目以“可继续修订、可审计、可稳定构建”为优先目标。正文不是单一大文件，而是由章节编排器和多个内容模块组成，因此修改时应同时维护论证内容、文件结构、文献键和后置材料之间的一致性。

## 1. 修改前先确定文件层级

- 全书结构与章节顺序：`textbook/main.tex`
- 九章主编排器：`textbook/chapters/revised/`
- 各章扩展模块：`textbook/chapters/` 下相应子目录
- 全局宏与版式：`textbook/style.tex`
- 参考文献：`textbook/references.bib`
- 练习提示与其他后置材料：`textbook/backmatter/`

如果只是修改某一论证、案例、文献说明或练习，优先编辑已有模块，不要为了很短的内容额外拆分文件。

## 2. 内容编辑原则

### 保持问题驱动

每章应围绕明确的问题推进，而不是把学者、流派或术语简单并列。新增内容应说明它解决什么问题、反驳什么主张，或改变了哪一步推理。

### 区分教材层次

尽量维持现有层次：基础正文负责建立问题与核心论证；扩展、研读、专题和深论模块负责增加争议、案例、技术细节和研究入口。避免把高密度专门争论直接塞进基础段落，导致难度突然跳升。

### 观点与评价分开

介绍一个立场时，先尽可能准确地重构其理由，再进入批评、比较或作者判断。对有争议的哲学主张，应避免把单一解释写成无争议事实。

### 术语保持一致

新增或修改核心术语时，检查：

- 正文首次出现处是否给出足够解释；
- `textbook/backmatter/glossary.tex` 是否需要同步；
- 是否应增加或调整索引标记；
- 中英文术语是否与其他章节保持一致。

## 3. 文献约定

所有正文使用的文献键必须存在于 `textbook/references.bib`。新增 BibLaTeX 条目后，应至少在正文论证或阅读地图中被实际引用。

提交前运行：

```sh
make check
```

检查脚本会同时发现“正文引用缺键”和“书目条目完全未使用”两类问题。

不要为了让检查通过而机械删除可能即将使用的研究资料；如果某条目确实是工作性储备而非正式书目，优先放到研究笔记或外部资料区，而不是正式 `references.bib`。

## 4. 练习与提示必须成对维护

修改章末练习数量或顺序时，同步更新：

```text
textbook/backmatter/exercise-hints.tex
```

每章练习与提示数量应一致；必要时也检查题号、概念范围和提示是否仍对应原题。

## 5. 文件结构约定

- 新建 TeX 模块时，延续现有文件首行的归属注释格式。
- 调整 `\input` / `\include` 时确认目标文件存在，避免悬空引用。
- 不提交生成的 PDF、`.aux`、`.bbl`、`.bcf`、`.idx`、`.ind`、`.log` 等构建产物。
- 外部参考 PDF 或扫描资料只放在 `reference/`，不要让它们进入教材构建依赖。

## 6. 提交前检查

至少运行：

```sh
make check
```

涉及版式、宏、章节结构、交叉引用、索引或参考文献格式时，还应运行：

```sh
make pdf
```

并检查最终日志中是否出现未定义引用、缺失文献键、索引错误或明显排版警告。

## 7. Commit 与 Pull Request

建议一个提交只处理一类问题，例如：

- `docs:` 项目文档、编辑说明；
- `content:` 教材正文或案例；
- `refs:` 书目与引用；
- `style:` LaTeX 宏和版式；
- `chore:` 构建、检查、仓库维护。

Pull Request 描述应至少说明：

1. 改了什么；
2. 为什么改；
3. 是否改变章节结构或核心论证；
4. 是否新增/删除文献；
5. 已运行哪些检查。

## 8. 出版元数据

除非明确进入出版或公开发布阶段，否则不要顺手填写或改变作者署名、封面日期、公开链接和 `website.yaml` 的 `publish` 开关。这些属于需要单独确认的发布决策。
